# Raw full-device logcat dumps (huge, mostly unrelated system noise; the
# relevant excerpts are already captured in reports/)
logs/

# Screenshot with a visible account email, excluded from the repo
screenshots/soc_fix_final.png

# Native build intermediates, if ndk-build is re-run in module/jni
module/jni/obj/
module/jni/libs/

# Video production materials — internal, not part of the released project
YouTube Video/
